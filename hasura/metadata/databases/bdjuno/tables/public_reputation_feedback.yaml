table:
  name: reputation_feedback
  schema: public
select_permissions:
  - role: anonymous
    permission:
      allow_aggregations: true
      columns:
        - id
        - creator
        - network
        - fb_type
        - dst_account
        - tx_id
        - ref
      filter: {}
      limit: 100