table:
  name: token_pending_clawbackable_operations
  schema: public
select_permissions:
  - role: anonymous
    permission:
      allow_aggregations: false
      columns:
        - id
        - from
        - to
        - amount
        - clearing_timestamp
      filter: {}
      limit: 100