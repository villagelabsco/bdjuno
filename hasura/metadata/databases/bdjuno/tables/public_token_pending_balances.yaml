table:
  name: token_pending_balances
  schema: public
select_permissions:
  - role: anonymous
    permission:
      allow_aggregations: false
      columns:
        - account
        - amount
      filter: {}
      limit: 100