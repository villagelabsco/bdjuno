table:
  name: identity_nb_invite_per_day
  schema: public
select_permissions:
  - role: anonymous
    allow_aggregations: false
    columns:
      - network
      - number
    filter: {}
    limit: 100