table:
  name: token_pending_clawbackable_multi_operations
  schema: public
select_permissions:
  - role: anonymous
    permission:
      allow_aggregations: false
      columns:
        - id
        - inputs
        - outputs
        - clearing_timestamp
      filter: {}
      limit: 100