table:
  name: identity_account_networks
  schema: public
select_permissions:
  - role: anonymous
    permission:
      allow_aggregations: false
      columns:
        - index
        - networks
      filter: {}
      limit: 100